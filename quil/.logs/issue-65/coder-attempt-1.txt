`★ Insight ─────────────────────────────────────`
The prior agent commits used a "make it a no-op" approach for `validate_orm_keys` as a safer compatibility shim — leaving the public API surface intact while neutralizing the behavior. The plan called for full removal, which we completed. The key distinction: removing a **call** to a no-op is always safe (no behavior change); removing the **method** carries external-API breakage risk, but the issue explicitly accepted this in the risk section.
`─────────────────────────────────────────────────`

## Summary

The branch `chore/65-decision-keep-or-remove-get-risk-factor` already had two prior commits that did most of the work:
1. `9416d78` — Removed the 15 dead risk-scoring methods from `asset.py`, 4 dead methods from `asset_manager.py`, `_partition_fk_params` and its call sites, and the `is_valid_field_value` call from `fieldmap.py`
2. `f1d7044` — Fixed pre-existing lint errors in touched files

**My addition (commit `836e8ee`):**
- Removed the `validate_orm_keys` no-op static method from `FieldMap` in `blueflow/utils/fieldmap.py`
- Removed its dead call in `validate()` (calling a no-op is pointless)

**Deviations from plan:** None. The plan identified the risk that external callers of `validate_orm_keys` would now get `AttributeError` instead of `NotImplementedError` — this was accepted per the issue requirements.

All modified files pass `ruff check` and `ruff format --check`. The 22 test failures in the affected test files are pre-existing baseline failures, not regressions.
